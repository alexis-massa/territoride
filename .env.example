DEBUG=True
SECRET_KEY=change-me
ALLOWED_HOSTS=localhost,127.0.0.1

POSTGRES_DB=territoride
POSTGRES_USER=territoride
POSTGRES_PASSWORD=change-me
POSTGRES_HOST=db
POSTGRES_PORT=5432

# From https://www.strava.com/settings/api — set the app's "Authorization
# Callback Domain" to localhost for local dev
STRAVA_CLIENT_ID=
STRAVA_CLIENT_SECRET=

# Any random string - used to verify the webhook subscription handshake
STRAVA_WEBHOOK_VERIFY_TOKEN=change-me

# Generate with: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
TOKEN_ENCRYPTION_KEY=change-me
